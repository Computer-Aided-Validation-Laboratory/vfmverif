plateWidth = 25.0
plateHeight = 35.0

centX = ${fparse 0.0}                 # mm
centY = ${fparse plateHeight / 2}     # mm

stdX = ${fparse plateWidth / 2}       # mm
stdY = ${fparse plateWidth / 4}       # mm
