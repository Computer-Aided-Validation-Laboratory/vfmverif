plateWidth = 25.0e-3
plateHeight = 35.0e-3

centX = ${fparse 0.0}                 # m
centY = ${fparse plateHeight / 2}     # m

stdX = ${fparse plateWidth / 2}       # m
stdY = ${fparse plateWidth / 4}       # m
